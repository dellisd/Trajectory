{
  "name": "trajectory",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@deck.gl/core": "^7.2.2",
    "@deck.gl/mapbox": "^7.2.2",
    "@deck.gl/mesh-layers": "^7.2.2",
    "@loaders.gl/core": "^1.2.1",
    "@loaders.gl/obj": "^1.2.1",
    "browserify": "^16.5.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
